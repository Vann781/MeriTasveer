# Left empty on purpose: the app calls /api on its own origin.
# In development Vite proxies that to the backend (see vite.config.js).
# In production one service serves both, or the static host proxies /api.
# Only set this if the API really is on another origin.
VITE_API_URL=
